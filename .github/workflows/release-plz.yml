name: Release-plz

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-plz-${{ github.ref }}
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  release:
    name: Release tagged version
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
    steps:
      - name: Validate release bot token
        env:
          RELEASE_BOT_TOKEN: ${{ secrets.RELEASE_BOT_TOKEN }}
        run: |
          if [[ -z "$RELEASE_BOT_TOKEN" ]]; then
            echo "RELEASE_BOT_TOKEN must be configured so release-plz PRs and tags trigger downstream workflows." >&2
            exit 1
          fi

      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
          token: ${{ secrets.RELEASE_BOT_TOKEN }}

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Run release-plz release
        uses: release-plz/action@v0.5
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_BOT_TOKEN }}
        with:
          command: release

  release-pr:
    name: Update release pull request
    needs:
      - release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Validate release bot token
        env:
          RELEASE_BOT_TOKEN: ${{ secrets.RELEASE_BOT_TOKEN }}
        run: |
          if [[ -z "$RELEASE_BOT_TOKEN" ]]; then
            echo "RELEASE_BOT_TOKEN must be configured so release-plz PRs and tags trigger downstream workflows." >&2
            exit 1
          fi

      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
          token: ${{ secrets.RELEASE_BOT_TOKEN }}

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Run release-plz release-pr
        id: release-plz
        uses: release-plz/action@v0.5
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_BOT_TOKEN }}
        with:
          command: release-pr

      - name: Enable release PR auto-merge
        env:
          GH_TOKEN: ${{ secrets.RELEASE_BOT_TOKEN }}
          PR_JSON: ${{ steps.release-plz.outputs.pr }}
        run: |
          if [[ -z "$PR_JSON" || "$PR_JSON" == "null" ]]; then
            echo "No release PR to auto-merge."
            exit 0
          fi

          pr_number="$(jq -r '.number' <<<"$PR_JSON")"
          if [[ -z "$pr_number" || "$pr_number" == "null" ]]; then
            echo "release-plz did not return a release PR number." >&2
            exit 1
          fi

          gh pr merge "$pr_number" --auto --merge --delete-branch
